# -*- mode: snippet -*-
# name: type case switch
# key: typecase
# --
switch ${1:somevar} := $1.(type) {
case ${2:int}:
    ${3:fmt.Printf("The type of '%d' is %T\n", a, a)}
${4:default:
    ${5:fmt.Printf("The type of '%v' is %T\n", a, a)}}
}